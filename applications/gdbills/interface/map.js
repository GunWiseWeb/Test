/*
 * GD Bills — interface JS.
 *
 * The state-map tiles are plain anchors that navigate to ?state=XX and the
 * controller renders the bills inline alongside the Type/Date filter bar.
 * There is no modal/popup; nothing here intercepts the clicks. This file
 * is kept as an empty IIFE so the controller's js() enqueue (and any
 * future tile-level enhancement) has a stable mount point.
 */
(function () { 'use strict'; })();
